create table if not exists product
(
    id              bigint          primary key generated by default as identity,
    seller_id       bigint          not null references "user"(id),
    city_id         bigint          not null references city(id),
    created_at      timestamptz     not null,
    title           varchar(255)    not null,
    description     text,
    price           numeric(11, 2)  not null,
    sold            boolean         not null default false
);

comment on table product is 'Таблица, содержащая информацию о товарах';

comment on column product.id is 'Уникальный идентификатор товара';
comment on column product.seller_id is 'Внешний ключ на продавца (пользователя)';
comment on column product.city_id is 'Внешний ключ на город';
comment on column product.created_at is 'Дата создания объявления о товаре';
comment on column product.title is 'Название товара';
comment on column product.description is 'Описание товара';
comment on column product.price is 'Цена товара';
comment on column product.sold is 'Флаг проданного товара';