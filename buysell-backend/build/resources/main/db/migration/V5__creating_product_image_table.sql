create table if not exists product_image
(
    id              bigint          primary key generated by default as identity,
    product_id  	bigint          not null references product(id),
    is_main         boolean         not null,
    file_path       varchar(511)    not null,
    file_name       varchar(255)    not null,
    file_size       bigint          not null
);

comment on table product_image is 'Таблица, содержащая информацию об изображениях товаров';

comment on column product_image.id is 'Уникальный идентификатор изображения';
comment on column product_image.product_id is 'Внешний ключ на товар';
comment on column product_image.is_main is 'Флаг основного изображения';
comment on column product_image.file_path is 'Путь к файлу изображения';
comment on column product_image.file_name is 'Имя файла изображения';
comment on column product_image.file_size is 'Размер файла в байтах';