create table if not exists deal
(
    id                  bigint          primary key generated by default as identity,
    product_id      	bigint          not null references product(id),
    buyer_id            bigint          not null references "user"(id),
    created_at		    timestamptz     not null,
    is_approved         boolean         not null default false,
    state               varchar(255)    not null
);

comment on table deal is 'Таблица, содержащая информацию о сделках';

comment on column deal.id is 'Уникальный идентификатор сделки';
comment on column deal.product_id is 'Внешний ключ на товар';
comment on column deal.buyer_id is 'Внешний ключ на покупателя (пользователя)';
comment on column deal.created_at is 'Дата создания сделки';
comment on column deal.is_approved is 'Флаг подтверждения сделки';
comment on column deal.state is 'Статус сделки';